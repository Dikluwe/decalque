//! Composição do binário `decalque` (L4). O pipeline interno está disponível
//! na biblioteca; a interface de linha de comando ainda aguarda especificação.
//!
//! Sem cabeçalho de linhagem: nenhum prompt gera este ficheiro — é ficheiro
//! de composição (ADR 0003, excepção para ficheiros sem prompt de origem).

fn main() {
    println!("decalque: pipeline interno pronto; CLI ainda não especificada");
}
